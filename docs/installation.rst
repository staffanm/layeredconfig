============
Installation
============

At the command line::

    $ easy_install layeredconfig

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv layeredconfig
    $ pip install layeredconfig